name: CommandSystem
version: '1.0'
main: net.astopia.commandsystem.CommandSystemPlugin
description: CommandSystem
api-version: '1.21.4'
dependencies:
  bootstrap:
    PaperConnector:
      load: BEFORE
      required: true
      join-classpath: true
  server:
    PaperConnector:
      load: BEFORE
      required: true
      join-classpath: true