name: EssentialsA
version: '1.0'
main: net.unnamed.minecraft.paper.essentials.EssentialsPlugin
description: Essentials
api-version: '1.21.4'
dependencies:
  bootstrap:
    PaperConnector:
      load: BEFORE
      required: true
      join-classpath: true
    CommandSystem:
      load: BEFORE
      required: true
      join-classpath: true
  server:
    PlaceholderAPI:
      load: BEFORE
      required: true
      join-classpath: true
    PaperConnector:
      load: BEFORE
      required: true
      join-classpath: true
    Vault:
      load: BEFORE
      required: true
      join-classpath: true
    CommandSystem:
      load: BEFORE
      required: true
      join-classpath: true