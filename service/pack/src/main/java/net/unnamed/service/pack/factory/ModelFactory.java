package net.unnamed.service.pack.factory;

import net.kyori.adventure.key.Key;
import team.unnamed.creative.model.Model;

public class ModelFactory {
/*    public Model createModel(String namespace, String value) {
        Model.model().key(Key.key(namespace, value))

                // TODO: continue this you're going to be looking at this in the evening the plan was to make custom config handling for modules and fonts maybe
    }*/
}
